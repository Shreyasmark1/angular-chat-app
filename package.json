{
  "name": "chat-backend",
  "version": "1.0.0",
  "description": "simple chat app node , socket io, express",
  "main": "index.js",
  "scripts": {
    "start": "clear && echo Starting server && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  }
}
